
<%- include('../compartido/estilos-generales/estilos-generales-component') %>

<a href="/producto/vista/crear">
    <button>
        CREAR
    </button>
</a>

<% if(typeof parametrosConsulta != 'undefined') {%>
    <p> MENSAJE: <%= parametrosConsulta.mensaje%></p>
<% }%>

<table>
    <tr>
        <th>Nombre</th>
        <th>Acciones</th>
        <!--   ESTO ES PARA UNA VISTA DE ADMINISTRADOR YA QUE SOLO INGRESA LOS PRODUCTOS -->
    </tr>

    <% arregloProductos.forEach((objeto) => { %>
        <tr>
            <td><%= objeto.nombre %></td>
            <td>
                <button>Ver</button>
                <a href="/producto/vista/editar/<%=objeto.productoId%>">
                    <button>Editar</button>
                </a>
                <form action="/producto/eliminarDesdeVista/<%= objeto.productoId %>" method="post">
                    <button>Eliminar</button>
                </form>
            </td>
        </tr>
    <% }) %>

</table>
